/*console.log()-only prints the result.
Returns stores the result, so you can use it later*/


//Return statement 
function sum(a, b) {
    return a + b;           //Function body - it will take the value of a and b, and give the result back
}
let result = sum(10, 20);       //calling function
console.log(result);


function sub(a, b) {
    return a - b;               //Function body - it will take the value of a and b, and give the result back
}
console.log(sub( 20 , 5));
